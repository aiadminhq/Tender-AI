# 複製為 .env 後填入實際值：cp env.example .env
# .env 已被 .gitignore，勿入版控。本機 brew 開發預設值見 app/core/config.py，無 .env 亦可跑。
DATABASE_URL=postgresql+psycopg://tender:tender@localhost:5432/tenderai
OLLAMA_URL=http://localhost:11434
EMBED_MODEL=bge-m3
ANTHROPIC_API_KEY=        # 高品質推理/摘要；放系統 secret，勿入版控
PCC_SCRAPER_PATH=../tender-bot/tender_daily.py
APP_API_KEY=             # 簡易後端保護（前端/Cloudflare Access 帶入）
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173   # 前端允許來源（逗號分隔）；正式站填前端網域

# 白名單登入（@hqdesign.tw）：app 自簽 JWT。JWT_SECRET 留空時 /auth/login 一律 401，
# 正式環境務必設一組隨機長字串（例如 `openssl rand -hex 32`），勿入版控。
JWT_SECRET=
JWT_EXPIRE_MINUTES=720

# 複製為 .env 後填入實際值：cp .env.example .env（.env 已 gitignore，勿入版控）
# 本機開發：後端跑在 localhost:8000 時可不設，用預設值即可。
# 正式站（Vercel）：VITE_API_BASE 必須指向實際部署的 tender-ai-backend 網址，
# 否則登入與標案清單會連不到後端（fetch 失敗時前端僅 fallback 顯示內建 mock 資料）。
VITE_API_BASE=http://localhost:8000/api/v1

# 選用：後端有設 APP_API_KEY 時才需要帶。
VITE_API_KEY=

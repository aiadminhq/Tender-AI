# Tender AI frontend 環境變數範例
# 本機：複製成 .env.local 使用；正式值請在 Vercel Dashboard → Settings → Environment Variables 設定。
# 注意：VITE_ 前綴的變數會被打包進「瀏覽器 bundle」（公開可見）。
#       後端機密（簽章金鑰、資料庫連線、模型憑證…）一律不可帶 VITE_ 前綴、不放這裡。

# 後端 API base（需含 /api/v1）。本機可直接連 8000 或走 Vite 同源 proxy。
# production 固定使用同源 /api/v1，此值只供本機／preview 覆寫。
VITE_API_BASE=http://localhost:8000/api/v1

# 後端粗閘金鑰（對應後端 APP_API_KEY）；後端未強制時可留空
VITE_API_KEY=

# 是否實連後端：production 設 true；設 false＝純前端 mock 模式（完全不外連）
VITE_USE_API=true

# 備註：本前端「不直連 Supabase」（無 supabase-js）。Supabase 僅作為「後端」的 PostgreSQL，
#       由後端連線設定使用；前端只透過上面的 VITE_API_BASE 打後端 API。

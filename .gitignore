# ── OS ──────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ── 本機 agent / 工具狀態（不入版控）──────────────────
.agents/
.ccw/
.claude/*
# 例外：設計/品味技能組需入版控，雲端 session 才能自動取用
!.claude/skills/
.codegraph/
.workflow/

# ── 本機 plan bridge token（含一次性 127.0.0.1 token）──
**/.plan-url

# ── 其他專案產出（報表站台屬 tender-bot 產物，非本 repo 源碼）──
tender-reports/

# ── 前端（保險：巢狀 .gitignore 已涵蓋，這裡再兜一層）──
tender-ai-frontend/node_modules/
tender-ai-frontend/dist/
**/*.local

# ── 後端（保險：巢狀 .gitignore 已涵蓋）─────────────────
**/.venv/
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
**/.ruff_cache/
**/.mypy_cache/

# 環境與密鑰：永不入版控
**/.env
**/.env.*
!**/env.example
!**/.env.example

# Layer B 行為資料（評價/註記/決策向量，合作範圍內共享但不入版控）與 DB 匯出：永不入 repo
*.dump
*.sql.gz
**/data/private/
**/data/downloads/
**/backfill_report.json

# ── 設計參考圖庫（本機靈感素材，非源碼；視覺方向已被 plans/uiux-v2-draft.html 吸收）──
Dashboar UI REF/

# ── 本機工具設定（Preview MCP launch 設定，機器相關）──
tender-ai-frontend/.claude/
